const person = {
    userName: "Abhay",
    age: 19,
    isLoggedIn:true,
    userDetails(){
        consol.log(`The userName is ${this.userName} and the age of the victim is ${this.age}`)
    }
}

export default person